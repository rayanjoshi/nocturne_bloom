defaults:
  - _self_
  - model_CNNLSTM
  - data_module

trainer:
  project_name: "NVDA Stock Predictor"
  run_name: "CNN-LSTM"
  max_epochs: 200
  accelerator: "auto"
  devices: 1
  log_every_n_steps: 10
  seed: 1

optimizer:
  lr: 0.0001