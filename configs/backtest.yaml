defaults:
  - model_Ensemble

data_loader:
  TICKER: "NVDA"
  PERMNO: 86580
  GVKEY: 117768
  START_DATE: "2022-10-31" # Date should be from a quarter before start date, so PE and PB data can be calculated
  END_DATE: "2024-12-31"
  WRDS_USERNAME: "rj524"
  raw_data_path: data/raw/nvda_raw_evaluate_data.csv
  sql_save_path: ../src/WRDS_query.sql

features:
  preprocessing_data_path: data/preprocessing/nvda_preprocessed_evaluate_data.csv

data_module:
  window_size: 10
  target_col: "Target"
  feature_scaler_path: models/feature_scaler.pkl
  target_scaler_path: models/target_scaler.pkl
  x_scaled_save_path: data/processed/nvda_x_scaled_evaluate_data.pt
  y_scaled_save_path: data/processed/nvda_y_scaled_evaluate_data.pt

backtest:
  predictions_save_path: data/predictions/nvda_predictions.csv
  lookback_window: 30
  initial_cash: 10000
  strategy: "baseline"
